{
  "name": "bcmp",
  "private": true,
  "description": "Front-end assets for BCMP: the Tailwind + daisyUI stylesheet and the vendored HTMX and Alpine builds. Produced by the Node stage of the Docker build (ADR 0002); the runtime image keeps no Node.",
  "scripts": {
    "build": "npm run build:css && npm run build:js",
    "build:css": "tailwindcss --input assets/css/app.css --output static/css/app.css --minify",
    "build:js": "mkdir -p static/js && cp node_modules/htmx.org/dist/htmx.min.js static/js/htmx.min.js && cp node_modules/alpinejs/dist/cdn.min.js static/js/alpine.min.js",
    "watch:css": "tailwindcss --input assets/css/app.css --output static/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "alpinejs": "^3.15.12",
    "daisyui": "^5.7.16",
    "htmx.org": "^2.0.10",
    "tailwindcss": "^4.3.3"
  }
}
