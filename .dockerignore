.git/
.venv/
venv/
__pycache__/
*.py[cod]
populate/
populate_mock_data/
Jenkinsfile
.gitignore
pytest.ini
.pylintrc
.gitlab-ci.yml
.flake8
.DS_Store
*.swp

# Runtime artifacts / secrets — provided on the server, not baked in
.env
.env.*
data/
staticfiles/
db.sqlite3

# Build output — the asset stage produces its own copy, so a locally built or
# stale stylesheet (or vendored script) must never enter the build context (ADR 0002)
node_modules/
static/css/
static/js/
