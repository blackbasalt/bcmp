"""Аренда получает ссылку на договор, а свободный «номер договора» уходит.

Две половины одного решения, и потому одной миграцией (ADR 0032): рядом с настоящей связью
свободное поле стало бы второй правдой о том, к какой бумаге аренда относится, а две
миграции оставили бы между собой день, когда в базе стоят обе.

Ссылка необязательна, и это не послабление: в выгрузке УК колонки с номером договора нет
вовсе, и ни одна из 36 заведённых аренд бумаги назвать не может. Требовать договор значило бы
выдумать 36 неподписанных.

Столбец уносит значения, и все они — вымышленные: настоящие аренды приехали без номера, а
восемнадцать номеров наполнения `fill_leases` заводит заново — теперь договорами вида «Аренда
помещений», на которых его аренды и висят. Поэтому переноса здесь нет: переносить нечего, а
код, сочиняющий договор из строчки, стоял бы рядом с наполнением, которое делает то же самое,
и первый же расход между ними никто бы не заметил.

`SET_NULL` на связи — решение ADR 0034: удаление договора аренды не уносит, ссылка обнуляется,
аренды стоят.
"""

# Generated by Django 6.0.7 on 2026-09-16 13:04

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('documents', '0008_contract_terms'),
        ('leases', '0001_initial'),
    ]

    operations = [
        migrations.RemoveField(
            model_name='lease',
            name='contract_no',
        ),
        migrations.AddField(
            model_name='lease',
            name='contract',
            field=models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='leases', to='documents.document', verbose_name='договор'),
        ),
    ]
