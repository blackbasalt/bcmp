# bcmp

Django project holding паспорта зданий for commercial бизнес-центры — spaces, engineering
systems, documents and the parties involved. First stage of an ИИ-управляющий platform.

## Agent skills

### Issue tracker

GitHub Issues on `blackbasalt/bcmp`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical labels, unchanged. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
