"""Раздел «Договоры» — полка над строками, которыми владеет раздел документов.

Приложение Django без единой модели, ровно как `rooms` (ADR 0016): договор — это документ
вида «Договор» с таблицей условий сбоку (ADR 0030), и обе таблицы живут в `documents`, где
стоят и остальные спутники документа. Приложением этот пакет стал ради подсветки пункта
меню: `templates/shell.html` считает открытый раздел по `request.resolver_match.app_name`, и
полка, положенная внутрь чужого раздела, зажигала бы чужой пункт.

`contracts`, а не `obligations`: «договор» — слово самой управляющей компании, и оно же
стоит в `CONTEXT.md`, тогда как «обязательство» описывает то, что в договоре записано, а не
бумагу, которую ищут на полке.
"""
