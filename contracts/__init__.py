"""Раздел «Договоры» — пока одно правило рода, а полка и экран придут следом.

Приложением Django это станет тогда, когда появится полка: `templates/shell.html` считает
открытый раздел по `request.resolver_match.app_name` (ADR 0016), и раньше `urls.py` из этого
ничего не следует. Правило рода лежит здесь уже сейчас, потому что оно про договор, а не про
документ, и искать его будут там, где живёт всё остальное про договоры.
"""
