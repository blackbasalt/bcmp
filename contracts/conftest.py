"""Чем стоят на ногах тесты раздела «Договоры» — сверх того, что держит весь набор.

Корневой `conftest` держит организации, сотрудников, здания и саму фабрику договора:
`make_contract` нужен и полке, и экрану, и аренде, и полке помещений, и двух определений
одного договора быть не должно. Здесь стоит одно, о чём спрашивают обе створки этого
раздела и никто больше.

Сторона с учётной карточкой — это и есть то самое: контрагента выбирают из Сторон своей
учётной карточки (ADR 0020), и заведение договора, и правка его условий спрашивают один и
тот же набор. Стоявшая бы в каждом из двух наборов порознь, она дала бы двум створкам двух
разных «Альфа» — и отказ одной из них было бы нечем объяснить.
"""

import pytest

from parties.models import PartyRecord


@pytest.fixture
def counterparty(downtown, alpha):
    """ТОО «Альфа», которую DownTown Management знает: Сторона плюс учётная карточка на неё.

    Карточка здесь не украшение, а условие: контрагент выбирается из Сторон учётной карточки
    читателя, и Сторона без неё лежит в реестре, находимая при заведении аренды и не видная
    тому, кто заводит договор (ADR 0020).
    """
    PartyRecord.objects.create(party=alpha, org=downtown)
    return alpha
